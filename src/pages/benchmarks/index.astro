---
// Benchmarks — "How good is it actually?"
//
// Two things on one page, and the order is deliberate: the tracked
// measurements first (the data is the section; the posts are a reading
// of it), then any benchmark posts underneath.
//
// PHASE 1: the listing only. The tracker table, its `measuredBy` badges
// and the CC-BY attribution land in Phase 4, into the default slot
// below — the collection is defined but empty until then.
import SectionIndex from "../../components/SectionIndex.astro";
---
<SectionIndex section="benchmark" />
