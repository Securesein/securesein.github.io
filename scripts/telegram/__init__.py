"""
The private editorial surface (brief §11).

The public site carries no feedback controls and needs no accounts; all
of that lives in one Telegram chat with the owner.

**Feedback is steering, not gating.** It arrives after publication and
never blocks it. Nothing in this package can hold a post back, and
nothing in the pipeline waits on a reply — if it could, the digest
would be an approval queue with extra steps, which §2 rules out.
"""
